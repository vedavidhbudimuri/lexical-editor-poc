import * as React from 'react'
import ReactHorizontalScrollingMenu from 'react-horizontal-scrolling-menu'

// https://github.com/asmyshlyaev177/react-horizontal-scrolling-menu

function HorizontalScrollList(props) {
   return <ReactHorizontalScrollingMenu {...props} />
}

export default HorizontalScrollList
